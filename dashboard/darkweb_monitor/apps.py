from django.apps import AppConfig

class DarkwebMonitorConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'darkweb_monitor'
    verbose_name = 'Darkweb_monitor'  # Pastikan nama sesuai dengan filter di template
